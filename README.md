# Анализ отзывов на рецепты: Задача классификации и кластеризации

Этот проект посвящен анализу данных о кулинарных рецептах и отзывах пользователей. В работе исследуется применимость методов машинного обучения для предсказания пользовательского рейтинга.

## 🎓 Соответствие требованиям зачёта

*   **Нетривиальность:** Использован реальный многомерный датасет *Recipe Reviews and User Feedback* с разнотипными признаками.
*   **Объем данных:** Выборка составляет **18 182** объекта (минимум по заданию — 1000).
*   **Качество анализа:** Проведен полный цикл от EDA и предобработки до сравнения обучения с учителем и без учителя.
*   **Аргументация:** Выбор метрик и методов обоснован спецификой данных (дисбаланс классов, чувствительность к масштабу).

---

## 🔍 Детальный отчет

### 1. Содержательное описание задачи
Цель работы — построить модель, способную классифицировать отзывы пользователей по уровню рейтинга. Это классическая задача анализа потребительского поведения, которая может быть использована для фильтрации контента или рекомендаций.

### 2. Что предсказываем?
Целевая переменная — рейтинг рецепта (`Rating`). Это дискретная величина, принимающая значения 0, 1, 2, 3, 4 и 5.

### 3. Каков объем данных ($N$)?
Размер датасета составляет **18 182** записи.

### 4. Сколько признаков ($d$)?
В финальной модели после очистки данных от неинформативных колонок (ID, имена, даты, тексты) используется **7 ключевых признаков**.

### 5. Сколько классов ($K$)?
Всего **6 классов** (от 0 до 5 звезд включительно).

### 6. Какой тип признаков?
*   **Числовые (Numeric):** репутация пользователя, количество ответов, лайки, дизлайки, лучший счет (`best_score`).
*   **Категориальные (Categorical):** название рецепта (обработано через `One-Hot Encoding`).

### 7. Были ли пропущенные значения?
В признаках, используемых для обучения, пропусков нет. В исходных данных были единичные пропуски в текстовых полях, но эти столбцы не участвовали в работе модели.

### 8. Сбалансированы ли классы?
**Нет**, наблюдается сильный дисбаланс. Класс «5» доминирует и составляет **76.06%** выборки. Остальные классы представлены значительно слабее (например, оценка «2» — всего 1.27%).

### 9. Диаграммы рассеивания и матрица корреляции
*   **Корреляция:** Выявлено, что признаки `best_score` (0.50) и `thumbs_up` (0.33) имеют наиболее заметную линейную связь с рейтингом.
*   **PCA-диаграмма:** Визуализация в 2D-пространстве главных компонент показала, что объекты разных классов сильно накладываются друг на друга, не образуя изолированных групп.

### 10. Какие выводы?
Рейтинг — это субъективный параметр. На основе только метаданных отзыва (без анализа текста) предсказать точную оценку сложно. Модель склонна переобучаться на мажоритарный класс (5 звезд).

### 11. Какую делали предобработку?
1.  Удаление нерелевантных данных (ID, временные метки).
2.  Кодирование категориальных признаков (`OneHotEncoder`).
3.  Нормализация данных (`StandardScaler`), что критично для алгоритмов, основанных на расстоянии (KNN).
4.  Использование `Pipeline` для предотвращения утечки данных при кросс-валидации.

### 12. Какой метод оказался лучшим?
Метод **K-ближайших соседей (KNN)** с подобранным гиперпараметром $k=19$.
*   **Метрика:** Accuracy на тесте составила **0.7685**.

### 13. Какой метод оказался худшим?
Методы кластеризации (в частности, **K-Means** и **Agglomerative Clustering**).
*   **Метрика:** Индекс ARI оказался близок к нулю (**-0.0032**), что говорит о том, что естественное разбиение данных на группы не совпадает с логикой выставления оценок пользователями.

### 14. Помог ли PCA?
PCA помог в визуальном анализе. Он наглядно подтвердил отсутствие четкой структуры в данных и объяснил, почему кластеризация не сработала. Без PCA было бы сложнее аргументировать выбор в пользу обучения с учителем.
